<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.education.centredetri">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/image4"
        android:label="@string/app_name"
        android:roundIcon="@drawable/image4"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splach">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".acceuil" />
        <activity android:name=".compteCentre.sinscrire" />
        <activity android:name=".connexion" />
        <activity android:name=".compteCentre.compteCentre" />
        <activity android:name=".evenement.ListeEvenement"></activity>
    </application>

</manifest>